{
  "name": "smartstock-guinee",
  "version": "1.0.0",
  "description": "SmartStock Guinée - Fullstack",
  "scripts": {
    "dev": "concurrently --names \"API,UI\" --prefix-colors \"cyan,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "migrate": "npm run migrate --prefix backend && npm run migrate2 --prefix backend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
